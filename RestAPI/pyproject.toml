[project]
name = "VideoStreamAPI"
version = "0.1.0"
description = "Video Streamer backend. Flask restx."


[tool.poetry]
packages = [{ include = "VideoStreamAPI"}]


[build-system]
build-backend = "flit_core.buildapi"
requires = ["flit_core >=3.2,<4"]