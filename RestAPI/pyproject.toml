[project]
name = "VideoStreamAPI"
version = "0.1.0"
description = "Video Streamer backend."


[tool.pytest.ini_options]
pythonpath = ["."]

[build-system]
build-backend = "flit_core.buildapi"
requires = ["flit_core >=3.2,<4"]